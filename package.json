{
  "name": "tribucket",
  "version": "3.3.0",
  "description": "Lightweight portable package manager",
  "main": "src/index.ts",
  "bin": {
    "tribucket": "src/index.ts"
  },
  "scripts": {
    "build": "bun build src/index.ts --compile --outfile tribucket",
    "build:debug": "bun build src/index.ts --compile --define DEBUG_BUILD=true --outfile tribucket-debug",
    "build:web": "bun run website/build.ts",
    "dev": "bun run src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "commander": "^15.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.3"
  }
}
